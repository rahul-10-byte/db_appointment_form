<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sticky Sidebar Toggle Button</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <style>
        body {
            display: flex;
        }
        .sidebar {
            width: 250px;
            height: 100vh;
            position: fixed;
            background-color: #343a40;
            transition: width 0.3s;
        }
        .sidebar.collapsed {
            width: 80px;
        }
        .sidebar a {
            color: #fff;
            display: block;
            padding: 15px;
            text-decoration: none;
        }
        .content {
            margin-left: 250px;
            padding: 20px;
            width: calc(100% - 250px);
            transition: margin-left 0.3s, width 0.3s;
        }
        .content.collapsed {
            margin-left: 80px;
            width: calc(100% - 80px);
        }
        .toggle-btn {
            position: fixed;
            left: 0;
            bottom: 20px;
            transform: translateX(-100%);
            transition: transform 0.3s;
        }
        .toggle-btn.show {
            transform: translateX(0);
        }
    </style>
</head>
<body>
    <div class="sidebar" id="sidebar">
        <a href="#">Home</a>
        <a href="#">About</a>
        <a href="#">Services</a>
        <a href="#">Contact</a>
    </div>
    <div class="content" id="content">
        <h1>Main Content</h1>
        <p>This is the main content area.</p>
    </div>
    <button class="btn btn-primary toggle-btn" id="toggleBtn">Toggle Sidebar</button>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            var sidebar = document.getElementById('sidebar');
            var content = document.getElementById('content');
            var toggleBtn = document.getElementById('toggleBtn');

            toggleBtn.addEventListener('click', function() {
                sidebar.classList.toggle('collapsed');
                content.classList.toggle('collapsed');
            });

            // Show the toggle button when the sidebar is collapsed or expanded
            sidebar.addEventListener('transitionend', function() {
                if (sidebar.classList.contains('collapsed')) {
                    toggleBtn.classList.add('show');
                } else {
                    toggleBtn.classList.remove('show');
                }
            });

            // Initially show the toggle button
            toggleBtn.classList.add('show');
        });
    </script>
</body>
</html>
    